-- creates a record type, initializes it, modifies a field, and prints the result

type Person is record
    var name : string
    var age : integer
end

routine main() is
    var p : Person is {name: "Alice", age: 30}
    p.age := p.age + 1
    print p.name, p.age
end